# תבנית ליצירת שחזור חדש

זה הקובץ שאתה מעתיק לקלוד כשאתה רוצה להוסיף שחזור לארכיון.
**אל תבקש ממנו HTML.** בקש ממנו את קובץ ה-JSON — האתר כבר יודע לרנדר אותו.

---

## מה להדביק לקלוד

> אני מוסיף שחזור לארכיון השחזורים שלי. הנה חומר הגלם:
>
> [כאן: הדבק את השאלות / צלם את הדף / העלה קובץ]
>
> החזר לי **אך ורק קובץ JSON תקין** במבנה הבא, בלי טקסט נוסף ובלי גדרות קוד:
>
> ```json
> {
>   "id": "molecular-2026-moed-a",
>   "course": "molecular",
>   "part": null,
>   "title": "שחזור 2026 — מועד א׳",
>   "kind": "shichzur",
>   "year": 2026,
>   "moed": "א",
>   "added": "2026-08-01",
>   "note": "הערה כללית על השחזור, או null",
>   "questions": [
>     {
>       "q": "טקסט השאלה",
>       "opts": ["מסיח 1", "מסיח 2", "מסיח 3", "מסיח 4"],
>       "a": 2,
>       "topic": "שם הנושא — למשל 'אנזימים, קינטיקה ועיכוב'",
>       "source": "מאיזה שחזור השאלה הגיעה — למשל 'שחזור מ״ב'. אופציונלי.",
>       "explain": "2-5 משפטים שמלמדים למה הנכונה נכונה, ואז משפט קצר לכל מסיח: 'המסיח הראשון שגוי כי...'",
>       "note": "הערה לשאלה — קיצורים, או ציון שהשאלה מבוססת גרף. אופציונלי.",
>       "table": {
>         "headers": ["עמודה", "עמודה"],
>         "rows": [["תא", "תא"], ["תא", "תא"]]
>       }
>     }
>   ]
> }
> ```
>
> כללים:
> - `course` — מזהה המקצוע (באנגלית), למשל `biochem` / `molecular` / `physics`.
>   **המקצוע חייב להיות קיים ב-`exams/courses.json`** — אם הוא חדש, מוסיפים אותו שם קודם.
> - `part` — חלק בתוך המקצוע, למשל `"א׳"` / `"ב׳"`. אם למקצוע אין חלוקה, `null`.
>   **הכותרת לא צריכה לחזור על שם המקצוע** — בתוך דף ביוכימיה זה כבר ברור.
> - `a` הוא **אינדקס מבוסס-אפס** של התשובה הנכונה במערך `opts`. התשובה הראשונה היא 0.
> - `opts` — **בשחזור אמיתי: כמו במקור, מילה במילה.** גם אם התשובה הנכונה בולטת
>   באורכה — ככה זה נראה במבחן, ואת זה בדיוק צריך לתרגל.
>   **בשאלות תרגול שאתה כותב (`kind: practice`) הכלל הפוך:**
>   - **כל התשובות באותו אורך בערך**, באותה רמת פירוט ובאותו מבנה תחבירי.
>     אם קיצרת אחת — קצר את כולן. תשובה שבולטת באורכה נענית בלי ידע.
>   - **בלי סוגריים של הסבר בתוך התשובה הנכונה** — `(כלומר…)`, `(שכן…)`.
>     ההצדקה שייכת ל-`explain`. סוגריים שקיימים רק בנכונה הם רמז מובהק.
>   - **הנכונה לא נכתבת בלשון בוראה** — לא הזהירה יותר ולא המדויקת יותר.
>   - **המסיחים חייבים לבלבל באמת**: טעות שסטודנט עושה, בלבול בין שני מושגים
>     קרובים, היפוך סיבה ותוצאה. מסיח אבסורדי מצמצם את השאלה לשלוש אפשרויות.
>   - **פיזור סטוכסטי:** מקום התשובה הנכונה מתפזר על כל המקומות בערך שווה.
>     ספור בסוף — בנק של 40 שאלות עם 4 תשובות: ~10 בכל מקום.
> - `topic` — **שווה מאוד להוסיף.** כשיש תיוג נושא, האתר מציג בסוף המבחן פילוח
>   שמראה באילו נושאים נפלת, ומאפשר תרגול חופשי לפי נושא.
>   **חשוב: השתמש באותם שמות נושא שכבר קיימים בארכיון** — נושא שנקרא "אנזימים"
>   בקובץ אחד ו"קינטיקה אנזימטית" בקובץ אחר יתפצל לשני תגים ויהרוס את הסינון.
> - `explain` — **החלק החשוב ביותר בשאלה.** מוצג אחרי המענה, וזה הרגע שבו לומדים.
>   מחרוזת אחת, שני חלקים בסדר הזה:
>   1. **ההסבר של התשובה הנכונה — 2–5 משפטים שמלמדים.** המנגנון או העיקרון,
>      מאיזה חוק זה נגזר, ונוסחה רלוונטית בתוך הטקסט. משפט אחד יבש אינו מספיק.
>      **אל תפתח ב"התשובה הנכונה נכונה כי"** — האתר מציג את זה מתחת לכותרת
>      "למה זה נכון", אז פתח בתוכן.
>   2. **פסילה קצרה לכל מסיח** — משפט אחד לכל אחד, עד ~20 מילים, שמתחיל
>      ב"המסיח ה\<סידורי\>" ואומר **מה הטעות**. משפט נפרד לכל מסיח.
>   **המספור: "המסיח השני" = התשובה השנייה במערך `opts`, 1-בסיס, כולל הנכונה.**
>   אם `a: 1` הפסילות הן הראשון/השלישי/הרביעי, ואין "המסיח השני".
>   האתר מפרק את זה לתבנית מוצגת (הסבר + בולט ממוספר לכל מסיח, והמסיח שנבחר
>   מסומן). בלי Markdown בתוך המחרוזת. הפירוט המלא: `QUESTION-STANDARD.md`.
> - `note` ו-`table` אופציונליים — השמט אותם לגמרי אם אין בהם צורך.
> - `kind` הוא אחד מ: `shichzur` (שחזור אמיתי) / `practice` (שאלות תרגול שהמצאת) /
>   `highyield` (נושאים חוזרים — נבנה אוטומטית, לא כותבים ביד) / `cards` (כרטיסיות
>   קריאה מהמרצה) / `guide` (מפת חומרים) / `case` (מקרים מתגלגלים) / `shinun` (שינון בעל־פה).
>   שלושת האחרונים אינם שאלות ולכן לא נספרים כ"שאלות" באתר.
> - `id` באנגלית בלבד, מקפים במקום רווחים, ייחודי בארכיון.
> - אם שאלה מבוססת על גרף או תמונה שאין לך — כתוב את זה ב-`note` של השאלה.

---

## אחרי שקיבלת את ה-JSON

1. שמור אותו בתיקייה `exams/` בשם `<id>.json` — למשל `exams/molecular-2026-moed-a.json`
2. הרץ בטרמינל, מתוך תיקיית הפרויקט:
   ```
   node sync.js
   ```
   הסקריפט יבדוק שהקובץ תקין (שאין `a` שמצביע לתשובה לא קיימת, שאין מזהה כפול, וכו')
   ויעדכן את `exams/manifest.json` לבד.
3. רענן את האתר. השחזור שם.

זהו. אין שלב שלישי.

---

## מקצוע חדש?

זה הקובץ להוספת **מבחן**. להוספת **מקצוע** — כרטיס המקצוע, רשימת הנושאים, מפת
החומרים והסיכום — ראו **[NEW-COURSE.md](NEW-COURSE.md)**.

---

## i❤️Shinun — אופציונלי, לפי אופי המקצוע

שווה במקצוע עם הרבה עובדות שאין מהן מה להסיק — שמות חלבונים, רעלנים, בופרים.
במקצוע חישובי או הבנתי הוא לא בהכרח מוסיף. הקובץ: `exams/<course>-shinun.json`
(`kind: "shinun"`). זה הופך אוטומטית לכרטיס "🧠 i❤️Shinun" בעמוד המקצוע, בלי קוד.
המבנה: עובדות אטומיות (מונח→עובדה) מקובצות ל-`groups`, לא שאלות.

```json
{
  "id": "<course>-shinun", "course": "<course>", "kind": "shinun",
  "title": "i❤️Shinun — מה שצריך בעל־פה",
  "heroSub": "משפט קצר על מה משננים כאן",
  "groups": [
    { "label": "שם הקבוצה (לניווט)", "items": [
      {
        "front": "הרמז (שם חלבון / תרופה / קיצור)",
        "back": "העובדה — משפט אחד קצר, בלי הסבר עמוק",
        "topic": "הנושא (אופציונלי, לחיבור מהסיכום)",
        "family": "אשכול תשובות בנות־בלבול — קריטי למצב המבחן",
        "mnem": "מנמוניקה (אופציונלי)"
      }
    ]}
  ]
}
```

כללים:
- **`family` הוא הלב.** מצב המבחן (MCQ) שולף מסיחים **רק מאותה `family`** — תשובות
  בנות־בלבול אמיתיות (כל חלבוני SNARE יחד, כל הבופרים יחד). פריט בלי משפחה של ≥2
  חברים לא נכלל במבחן — יופיע רק בהיפוך ובכסה־וגלה. אל תמציא משפחות מלאכותיות.
- `back` אטומי: משפט אחד. ההבנה העמוקה שייכת לכרטיסיות/לסיכום, לא כאן.
- **נוסחאות לא חובה** — אם יש דף נוסחאות נפרד, אין צורך לשכפל אותן לשינון.
- שאיבה מהקיים עדיפה על המצאה: מילון/כרטיסיות של המקצוע הם מקור טבעי.
- אחרי כתיבה: `node sync.js` (מאמת front/back לכל פריט) ורענון.
