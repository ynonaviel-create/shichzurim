# תדריך — עימות קליני (שנה א׳, סמסטר ב׳ — השלמה)

קרא קודם את `docs/shana-a/README.md`.

**`id`:** `clinical` · קיים באתר, `status: done`. 6 בנקי `practice` (מודל 2020–2022,
מ״ז, שני בנקי AI), מקרים מתגלגלים, כרטיסיות, `about`. **אין** `topics`, מפה, לומדה.

## המצב (24/09)

241 שאלות · 0 בלי נושא · 0 בלי הסבר. אבל **18 תגיות עם כפילויות** — בדיוק
הפיצול ש-`topics` נועד למנוע:
`סרטן השד` (18) / `סרטן השד ו-BRCA` (17) · `אבחנה מבדלת` (14) / `אבחנה מבדלת ובדיקה גופנית` (11) /
`בדיקה גופנית והאזנה` (16) · `קרדיולוגיה` (28) / `אנטומיה של הלב` (4) / `מערכת ההולכה וקולות הלב` (8) ·
`נוירולוגיה - מנינגיטיס ופרכוסים` (10) / `נוירולוגיה - סינקופה` (4) · `רפואת ריאות` (22) / `דלקת ריאות` (12).

**הטיה:** `clinical-ai-set-2` 86% ו-`clinical-ai-set-1` 74% ו-`clinical-mem-zayin` 71%
„הנכונה הארוכה” (ביקורת 8/2026) — הבנקים פתירים בלי ידע. חובה לאזן (`kind:practice`).

## רשימת הנושאים המוצעת (11) — ⏸️ ממתינה לאישור ינון

מאחדת את 18 התגיות בלי לאבד אף שאלה:

1. `קרדיולוגיה: הלב, ההולכה וקולות הלב` ← קרדיולוגיה + אנטומיה של הלב + מערכת ההולכה וקולות הלב (40)
2. `אוטם ותסמונת כלילית חדה` (23)
3. `טרשת עורקים וגורמי סיכון` ← טרשת עורקים + גורמי סיכון (20)
4. `רפואת ריאות ודלקת ריאות` ← רפואת ריאות + דלקת ריאות (34)
5. `אסתמה ומחלות חסימתיות` (16)
6. `סרטן השד ו-BRCA` ← שתי תגיות השד (35)
7. `סוכרת` (8)
8. `נוירולוגיה: מנינגיטיס, פרכוסים וסינקופה` ← שתי תגיות הנוירולוגיה (14)
9. `בדיקה גופנית והאזנה` ← בדיקה גופנית והאזנה + אבחנה מבדלת ובדיקה גופנית (27)
10. `אבחנה מבדלת` (14)
11. `הדמיה` (10)

חלופה: לפצל 9/10 אחרת (לפי איבר ולא לפי מיומנות). ינון מכריע.

## סדר העבודה

1. אישור ← `topics` בכרטיס (יש כבר `about`), תיוג מחדש לפי הטבלה.
2. איזון „הנכונה הארוכה” בשלושת הבנקים — קיצור/הארכה של מסיחים לפי
   `QUESTION-STANDARD.md`, בלי לשנות תוכן רפואי; דו״ח ההטיה של sync חייב לצאת נקי.
3. `strict: true` ← sync נקי ← קומיט.
4. גל 2: מפה (אין) — יחידה לכל נושא, נקודות „מה באמת נשאל”. גל 3: לומדה (אין).
   הקורס בנוי על וינייטות — המקרים המתגלגלים הקיימים (`clinical-cases.json`) הם
   הנכס לגל 3, לאמת ב-`tools/ekronot/audit_play.py`.

## מצב

- 24/09: תדריך נכתב; רשימה מוצעת ממתינה לינון.
- 24/09 ערב: **הושלם.** 241/241; 18 תגיות → 11 נושאים (137 שאלות, 11 כרטיסיות, 4 מקרים); „הנכונה הארוכה” ב-ai-set-2/ai-set-1/mem-zayin ירדה מ-86/74/71% ל-4/4/3% בניסוח בלבד; `strict: true`; sync נקי.
  חוב: מקום התשובה הנכונה ב-ai-set-2 מוטה (9/26/13/1) — תיקון דורש סידור מחדש של מסיחים + עדכון המספור בהסברים; ההסברים בשלושת הבנקים לא בתבנית „המסיח ה…”.
