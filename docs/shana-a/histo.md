# תדריך — היסטולוגיה (שנה א׳, סמסטר א׳)

קרא קודם את `docs/shana-a/README.md`.

**`id`:** `histo` · **אייקון מוצע:** 🔬 · `year: 1, semester: "א"` · **קהל:** מחזור נ״ג.

> ⏳ **שלד — ממתין לגל 0.** סשן הסריקה (מקומי) ממלא את הסעיפים המסומנים TODO
> מתוך הדרייב. עד אז לא פותחים לקורס הזה סשן ייבוא.

## מה ידוע כבר

קבוצות תרגול 11–16. קורס של **זיהוי פרפרטים** — השאלות תלויות תמונה. גל 3: `play:true` hotspot / keyer („זהה את הרקמה”) הם הכלי הטבעי. תמונות מהשחזורים ← `assets/img/`.

## הנושאים הקנוניים — TODO (הרשימה נסגרת באישור ינון)

לגזור 10–25 נושאים מהסילבוס/מערך השיעורים של נ״ב (ואם יש — של נ״ג). שם נושא =
עוגן לכל החיים. להציע כאן; ינון מאשר פעם אחת, יחד עם שאר הקורסים.

- TODO

## סדר הייבוא — TODO

הנתיבים יחסית לתיקיית הקורס בדרייב (`MAP.md`).

| # | קובץ | מחזור/מועד | מפתח תשובות | טקסט/סריקה | ~שאלות | הערות |
|---|---|---|---|---|---|---|
| 1 | TODO | | | | | |

**לא לייבא:** TODO (ולמה).

**שחזורים שאין להם מפתח:** לייבא רק אם אפשר להכריע את התשובה מול סיכום-השדרה,
ואז `trust: "unverified"` + `note`.

## מקורות לכתיבת ההסברים — TODO

- **סיכום-השדרה:** TODO (מי כתב, כמה עמודים, האם יש טקסט).
- סיכומים משניים: TODO.
- מבנה המבחן (שאלות, זמן, מסיחים, האם יש דף נוסחאות): TODO — נכנס ל-`about` ול-`simExam`.

## קשרים לקורסים אחרים (`related`, גל 2)

פתולוגיה (ekronot-b · `patho`), אנטומיה (`anatomy`).

## מה לדווח למטה

כמה שאלות נכנסו לכל נושא · אילו שאלות סומנו כספק · סתירות מפתח↔הסבר · נושאים
שנראים מחוץ לחומר · קבצים שלא נקראו ולמה.

---

## מצב

- 24/09: שלד. ממתין לסריקה.
