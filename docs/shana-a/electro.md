# תדריך — אלקטרופיזיולוגיה (שנה א׳, סמסטר ב׳ — השלמה)

קרא קודם את `docs/shana-a/README.md`.

**`id`:** `electro` · קיים באתר, `status: active` (מועדים של נ״ב, 7–8/2026 — יעבור
לארכיון לבד). 20 שחזורים רשמיים 2021–2026 (verified), 3 בנקי תרגול, מפה, לומדה
מלאה, שינון, כרטיסיות. **אין** `about` ו-`topics`. הקורס הכי שלם בשנה א׳.

## המצב (24/09)

541 שאלות · 0 בלי נושא · **7 בלי הסבר**: `electro-2023-boch1` (2), `electro-2024-moed-a` (4),
`electro-2025-moed-b` (1).

## רשימת הנושאים המוצעת (12) — ⏸️ ממתינה לאישור ינון

**זהה ליחידות המפה והלומדה** — לא משנים שם:

`סידן, אקסוציטוזה ומחזור הווזיקולה` · `פוטנציאל הפעולה` · `תכונות פאסיביות של הממברנה` ·
`מבנה ותפקוד הנוירון` · `מנגנוני טרנספורט` · `שיטות מחקר` · `הסינפסה: המודל החשמלי` ·
`רצפטורים ותגובה פוסט-סינפטית` · `פלסטיות סינפטית` · `פוטנציאל מנוחה` ·
`התאוריה הקוונטאלית` · `תנועת חלקיקים ודיפוזיה`

⚠️ `NEW-COURSE.md` מתעד פיצול ידוע ב**שינון**: „פוטנציאל פעולה” / „ממברנה פסיבית”
מול השמות הקנוניים. כשמדליקים `strict`, sync יתפוס את זה — ליישר את
`electro-shinun.json` ואת הכרטיסיות לשמות שלמעלה.

## סדר העבודה

1. אישור ← `topics` + `about` (מבחן רשמי: ~45 שאלות, בחני אמצע 10 שאלות, דף נוסחאות — יש כבר `FORMULAS`).
2. 7 הסברים. 3. יישור תגיות בשינון/כרטיסיות. 4. `strict: true` ← sync נקי ← קומיט.
5. גל 4: כשנ״ג מגיע לסמסטר ב׳ — `dates` חדשים.

## מצב

- 24/09: תדריך נכתב; רשימה מוצעת ממתינה לינון.
- 24/09 ערב: **הושלם.** 541/541 עם הסבר; 41 פריטי שינון יושרו לשמות הקנוניים; `strict: true`; sync נקי.
  חוב: הסברים ישנים בבנקים הרשמיים בלי פסילות ממוספרות (2023-boch1: 8, 2024-moed-a: 28, 2025-moed-b: 32; חלקם קצרים) — עובדים, אבל לא בתבנית „למה השאר נפסלים”.
