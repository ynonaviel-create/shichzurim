# תדריך — פיסיקה לרפואנים ב׳ (שנה א׳, סמסטר ב׳ — השלמה)

קרא קודם את `docs/shana-a/README.md`.

**`id`:** `physics` · קיים באתר, `status: active` (מועדי נ״ב). הקורס האחרון שנבנה
לפי `NEW-COURSE.md`: `about`, מפה (21 יחידות), לומדה מלאה (21 איורים, 34 סרטונים),
שינון, `simExam`. **אין** `topics` (הרשימה הסגורה לא נכתבה לכרטיס).

## המצב (24/09)

341 שאלות · **36 בלי הסבר** (כולן `physics-nun-bet.json`, השחזור האמיתי היחיד) ·
**57 שאלות מתויגות לפי מקור ולא לפי נושא:** `שאלות הכיתה` (40), `ניסויי הכיתה (סרטונים)` (10),
`הוגשו למרצה` (7). כשמדליקים `strict` הן ייפלו — לתייג מחדש לנושא פיזיקלי אמיתי
(המקור כבר כתוב ב-`part`/`title` של הקובץ, לא צריך תגית בשבילו).

## רשימת הנושאים המוצעת (21) — ⏸️ ממתינה לאישור ינון

**זהה ליחידות המפה והלומדה** — לא משנים שם:

חשמל: `מטען, שדה ופוטנציאל חשמלי` · `חוק גאוס ויישומיו` · `קבלים — חיבור ואנרגיה` ·
`זרם, התנגדות וחוק אוהם` · `מעגלים וחוקי קירכהוף` · `מעגל RC` · `האקסון כמוליך — תיאוריית הכבל`
מגנטיות: `שדה מגנטי וכוח לורנץ` · `כוח מגנטי על תיל, ביו-סבאר ואמפר` · `אינדוקציה — פאראדיי-לנץ`
גלים: `תנועה הרמונית פשוטה` · `גלים, קול ואפקט דופלר` · `גלים אלקטרומגנטיים`
נוזלים: `לחץ וצפיפות` · `עקרון ארכימדס והציפה` · `מתח פנים וחוק לפלס` · `משוואת ברנולי וספיקה`
דימות: `הספקטרום האלקטרומגנטי והאפקט הפוטואלקטרי` · `רנטגן ו-CT` · `רפואה גרעינית ו-PET` · `MRI`

## סדר העבודה

1. אישור ← `topics` בכרטיס.
2. תיוג מחדש של 57 השאלות (כיתה/ניסויים/הוגשו) לנושאים שלמעלה.
3. 36 הסברים לשחזור נ״ב. מקור: הלומדה שלנו (`guides/physics-full.html`) — היא נכתבה
   מההקלטות; זה המקרה היחיד שבו הלומדה היא מקור לגיטימי להסבר.
4. `physics-experiments.json` — 80% „הנכונה הארוכה” (ביקורת 8/2026): לאזן.
5. `strict: true` ← sync נקי ← קומיט.
6. ⚠️ **גבול עם `physics-a`** (פיסיקה א׳, סמסטר א׳ החדש): כשתיסגר רשימת `physics-a`
   לוודא שאין נושא כפול, ו-`related` בין השניים.

## מצב

- 24/09: תדריך נכתב; רשימה מוצעת ממתינה לינון.
