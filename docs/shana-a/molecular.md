# תדריך — ביולוגיה מולקולרית תאית (שנה א׳, סמסטר ב׳ — השלמה)

קרא קודם את `docs/shana-a/README.md`.

**`id`:** `molecular` · קיים באתר, `status: done`. 10 שחזורים (מ״ו–נ״ב), מפה
(12 יחידות, 142 נקודות), High Yield, כרטיסיות. **אין** `about`, `topics`, לומדה.

## המצב (24/09)

467 שאלות · 0 בלי נושא · **36 בלי הסבר**: `molecular-nun-bet.json` (35/35 —
השחזור האחרון, יובא בלי הסברים) ו-`molecular-high-yield.json` (1).

## רשימת הנושאים המוצעת (12) — ⏸️ ממתינה לאישור ינון

**זהה ליחידות המפה הקיימת** (`exams/molecular-guide.json`), שכבר משמשות אנקי
וקישורים רוחביים מעקרונות המדע — לכן **לא משנים שם**:

`שעתוק` · `מחזור התא והתמיינות` · `אפיגנטיקה` · `נזקי דנ"א` · `הנדסה גנטית` ·
`מנגנוני איתות תאי` · `מבנה הגרעין` · `טופולוגיות של הדנ"א וטלומרים` · `שכפול` ·
`אברוני התא` · `תרגום` · `בקרה ברמת החלבון ואפופטוזיס`

(תגיות בפועל: שעתוק 91, אפיגנטיקה 57, מחזור התא 52, הנדסה גנטית 44, נזקי דנ"א 43,
מבנה הגרעין 35, טופולוגיות 34, שכפול 29, איתות 29, תרגום 23, אברונים 20, בקרה 10.)

⚠️ גבול עם `cellbio` (ביולוגיה של התא, סמסטר א׳ החדש): `אברוני התא` ו`מנגנוני איתות
תאי` עלולים להילמד בשני הקורסים. לתאם כשתיסגר רשימת `cellbio`.

## סדר העבודה

1. אישור ← `topics` + `about` (מה הקורס, המרצה, מבנה המבחן — מהשחזורים: ~60 שאלות, 4 מסיחים).
2. 35 הסברים לשחזור נ״ב + 1 ב-High Yield, לפי הסטנדרט. מקור: הסיכומים בדרייב +
   מסמכי הפתרונות של מ״ח/מ״ז (`sources/README.md` — „מכרה זהב”).
3. **באג תוכן פתוח:** `molecular-nun-alef-moed-a.json` שאלה 16 — מסיחים 3 ו-4 זהים
   והנכונה מצביעה על הכפול. להכריע מול המקור (כנראה אחד היה „החצי-שמרנית”).
4. `trust` חסר ב-`molecular-lab-2026-moed-a` ו-`molecular-mem-zayin-moed-b` — להשלים.
5. `strict: true` ← sync נקי ← מדגם ← קומיט. גל 3: לומדה (`guides/molecular-full.html`).

## מצב

- 24/09: תדריך נכתב; רשימה מוצעת ממתינה לינון.
