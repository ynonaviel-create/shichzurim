# שנה א׳ לארכיון — תוכנית העבודה (סמסטר א׳ מאפס, סמסטר ב׳ להשלמה)

נכתב 24/09/2026 (סשן ענן), לפי סריקת הריפו, מייל הרישום של הפקולטה, והכרעות ינון.
המסמך המרכז: מה יש, מה נבנה, מי עושה מה, ובאיזה סדר. סעיף „מצב נוכחי” בסוף
מתעדכן בכל סשן.

---

## 1. למה

מחזור נ״ג מתחיל עכשיו את שנה א׳, והאתר משרת היום רק את נ״ב (שנה ב׳ — עקרונות
המדע, גמורים ובאוויר). ההחלטה (ינון, 24/09): להעלות את כל תכני שנה א׳ — סמסטר
א׳ שאין לו כלום, וסמסטר ב׳ שיש לו חמישה קורסים עם חובות. אותו צינור שעבד
בעקרונות המדע: סריקה ← מיפוי ← שחזורים עם הסברים ← מפות ← לומדות ← אוויר.

**הכרעות ינון (24/09):**
- הסריקה רצה ב**סשן מקומי על המק**; הענן לא רואה את OneDrive.
- **סמסטר א׳ — תשעה קורסים.** **סמסטר ב׳ — אין קורסים חסרים**, רק להשלים את החמישה.
- **סדר: גלים.** קודם שחזורים+הסברים לכל הקורסים, אחר כך מפות, אחר כך לומדות.

---

## 2. הקורסים

### סמסטר א׳ (חדשים, `year:1, semester:"א"`)

| קורס | `id` | ידוע מראש | תדריך |
|---|---|---|---|
| ביוסטטיסטיקה | `biostat` | קבוצות תרגול 11–16 (מייל הפקולטה 7/9/25) | `biostat.md` |
| כימיה | `chem` | קבוצות תרגול 11–16 | `chem.md` |
| פיסיקה א׳ | `physics-a` | קבוצות תרגול 11–16; ההמשך הוא `physics` (פיסיקה ב׳) | `physics-a.md` |
| כימיה אורגנית | `organic` | קבוצות תרגול 11–18; שחזור נ״ב בגוגל דרייב (ירדן בייטנר, 2/2026) | `organic.md` |
| היסטולוגיה | `histo` | קבוצות תרגול 11–16; זיהוי פרפרטים ← תמונות | `histo.md` |
| אמבריולוגיה | `embryo` | — | `embryo.md` |
| אפידמיולוגיה | `epi` | — | `epi.md` |
| ביולוגיה של התא | `cellbio` | ההמשך הוא `molecular` | `cellbio.md` |
| אנטומיה | `anatomy` | זיהוי ← תמונות | `anatomy.md` |

מספרי הקורסים (`code`) — בקובץ `רשימת קורסי החובה לסמסטר א' תשפו – שנה א.docx`
שמצורף למייל של נטלי פסטרנק מ-7/9/2025 (Gmail), או בדרייב.

### סמסטר ב׳ (קיימים, `year:1, semester:"ב"`) — מצב 24/09

| קורס | שאלות | בלי הסבר | בלי נושא | `topics` | `about` | מפה | לומדה | strict |
|---|---|---|---|---|---|---|---|---|
| ביוכימיה `biochem` | 337 | **273** | **150** | ✗ | ✗ | ✗ | ✗ | ✗ |
| ביומול `molecular` | 467 | 36 | 0 | ✗ | ✗ | ✓ | ✗ | ✗ |
| אלקטרו `electro` | 541 | 7 | 0 | ✗ | ✗ | ✓ | ✓ | ✗ |
| פיסיקה ב׳ `physics` | 341 | 36 | 0 | ✗ | ✓ | ✓ | ✓ | ✗ |
| עימות קליני `clinical` | 241 | 0 | 0 | ✗ | ✓ | ✗ | ✗ | ✗ |

כולם `status: done` (של נ״ב) ובלי רשימת נושאים סגורה. `ROADMAP.md` סעיפים 1–2
היו „מוקפאים עד החלטת המחזור על תוכן שנה א׳” — **זו ההחלטה.** הפרטים והרשימות
המוצעות — בתדריך של כל קורס.

---

## 3. הסדר: ארבעה גלים (+ גל 0)

### גל 0 — הסריקה (סשן מקומי, קובץ-קובץ)

1. לאתר את תיקיית שנה א׳ בדרייב ולרשום את עץ התיקיות — **לפי תיקיות, לא לפי שמות**.
2. לכל אחד מ-14 הקורסים: מלאי **כל** קובצי המבחן/בוחן/שחזור (נתיב, גודל, מחזור,
   מועד, האם יש מפתח, טקסט או סריקה, כמה שאלות בערך), מלאי הסיכומים (מועמדים
   לסיכום-שדרה), סילבוס/מערך שיעורים אם קיים, ומה לא רלוונטי ולמה.
3. לכתוב `MAP.md` (טבלה לכל קורס) ולמלא את התדריך של כל קורס: סדר ייבוא ממוספר
   עם נתיבים, „לא לייבא”, סיכום-השדרה המוצע, **הצעת רשימת נושאים** (10–25).
4. להעתיק את קובצי המקור של כל קורס ל-`sources/shana-a/<קורס>/` (לא בגיט).
5. דיווח: כמה קבצים, כמה מבחנים לכל קורס, איזה קורס עני בחומר.

**מוכן כש:** לכל 14 הקורסים שורה ב-MAP ותדריך מלא; רשימות הנושאים מוצגות לינון
**בפעם אחת** לכל הקורסים יחד.

### גל 1 — השחזורים וההסברים (80% מהערך)

**סמסטר א׳, סשן לכל קורס:**
1. `python3 new-course.py init <id> --name … --icon … --blurb … --year 1 --semester א` — כרטיס `soon`+`strict`.
2. `topics` המאושרים, `about`, `code`; `dates` כשיפורסמו מועדי נ״ג (עד אז `soon`).
3. ייבוא לפי התדריך עם `import-shichzur`; אין שחזורים ← בנק `practice` לפי `QUESTION-STANDARD.md`.
4. `node sync.js` נקי לפני כל קומיט.

**סמסטר ב׳ — מתחיל מיד, גם מהענן, לא תלוי בדרייב:** רשימות נושאים סגורות ו-`about`
לחמישה ← אישור ינון ← תיוג ← הסברים (ביוכימיה קודם: 273) ← איזון „הנכונה הארוכה”
בבנקי ה-AI ← `strict:true` על החמישה כשהם נקיים ← מדגם רפואי לינון.

**סדר הקורסים בגל 1:** לפי לוח המבחנים של נ״ג — הקורס שנבחן ראשון קודם. עד שיש
לוח: לפי עושר החומר במפה (קורס עם שחזורים מאומתים לפני קורס שצריך בנק תרגול).

**מוכן כש:** `node sync.js` נקי · לכל נושא ≥8 שאלות · 100% הסבר+נושא · דו״ח הטיה נקי.

### גל 2 — המפות והחזרות

- `new-course.py scaffold <id>` ← מילוי היחידות: תמצית, מקור ראשי מסיכום-השדרה,
  מלכודות, נקודות „מה באמת נשאל” עם qid-ים אמיתיים (הפורמט: `docs/ekronot/BUILD.md` סעיף 2).
- מפה ל-`biochem` ו-`clinical` (אין להם); רענון `molecular`/`electro`/`physics` מול הרשימות החדשות.
- רישום כל קורס עם ≥2 מחזורים ב-`COURSES` של `repeats.js` ← High Yield אוטומטי.
- `related` בין שנה א׳ לשנה ב׳: ביולוגיה של התא ↔ ביומול/פתולוגיה · כימיה ↔ ביוכימיה ·
  פיסיקה א׳ ↔ אלקטרו/פיסיקה ב׳ · היסטולוגיה ↔ פתולוגיה · אפידמיולוגיה ↔ ביוסטטיסטיקה.

### גל 3 — לומדות, סימולציה, שכבות

- `studyDoc` לכל קורס לפי `write-study-guide` (`build.py` ← תוכן ← `inject.py`).
  קודם מי שאין לו: biochem, molecular, clinical; ואז החדשים לפי סדר המבחנים.
- `simExam` כשמבנה המבחן ידוע מהשחזורים.
- שינון/אנקי/„🎮” לפי אופי הקורס: היסטולוגיה ואנטומיה — זיהוי מתמונה (hotspot/keyer);
  כימיה אורגנית — מנגנונים; ביוסטטיסטיקה — תרגילי חישוב (`DRILLS`, דורש קוד — המטה).
- סדר הוויתור אם הזמן לוחץ: אנקי ושינון ← לומדות ← סימולציה. **על שחזורים והסברים לא מוותרים.**

### גל 4 — עלייה לאוויר לנ״ג

- `dates` של נ״ג ← `status: active`. עד אז `soon` (הכרטיס נראה, בלי ספירה לאחור).
- חמשת קורסי סמסטר ב׳ נשארים `done` עד סמסטר ב׳ של נ״ג; אז `dates` חדשים והסרת `status`.
- `teaching` (תחילת ההוראה + נושאי כל שבוע) — מדליק „השבוע בקורס”.
- דף הבית יציג שני מדפים פעילים (שנה א׳ · סמסטר א׳ / שנה ב׳ · סמסטר א׳) לכולם.
  אם זה מפריע — „השנה שלי” במנוע; לא לפני שיש תוכן.
- מדגם לינון ← `node sync.js` ← קומיט ← `main` ← הודעה לנ״ג.

---

## 4. מה שעוד לא הוכרע

| # | שאלה | מי |
|---|---|---|
| 1 | מועדי הבחינות של נ״ג לסמסטר א׳ | ינון, כשיפורסם |
| 2 | רשימות הנושאים (14 קורסים) — פעם אחת אחרי גל 0; סמסטר ב׳ אפשר כבר עכשיו (בתדריכים) | ינון |
| 3 | סדר הקורסים בגל 1 | לפי לוח נ״ג / אחרי גל 0 |
| 4 | „השנה שלי” בדף הבית | נדחה |
| 5 | שם תיקיית שנה א׳ בדרייב ומספרי הקורסים | סשן הסריקה |

---

## 5. הפרומפטים

**סשן הסריקה (מקומי, פעם אחת):**
```
קרא את docs/shana-a/README.md ואת docs/shana-a/תוכנית-עבודה.md, וסרוק את תיקיית
שנה א׳ בדרייב לפי גל 0. התוצר: docs/shana-a/MAP.md + תדריך לכל קורס.
```
**סשן קורס (×14):**
```
קרא את docs/shana-a/README.md ואת docs/shana-a/<קורס>.md ותעבוד לפיו.
```

---

# מצב נוכחי — 24/09/2026

- `docs/shana-a/` נוצר: פרוטוקול, תוכנית, 14 תדריכים (9 שלדים לסמסטר א׳ שממתינים
  לסריקה; 5 תדריכים מלאים לסמסטר ב׳ עם רשימות נושאים מוצעות).
- **ממתין לינון:** רשימות הנושאים של חמשת קורסי סמסטר ב׳ (בתדריכים). אחרי אישור —
  ביוכימיה ראשונה (150 תיוגים, 273 הסברים).
- **ממתין לסשן המקומי:** גל 0.
