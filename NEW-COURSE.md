# העלאת קורס חדש לארכיון

זה הסדר שבו נבנה קורס חדש, ומה חייב להיות בו לפני שהוא עולה לאוויר. הוא נגזר
מפיזיקה ב׳ — הקורס האחרון שנוסף — ומכל מה שנשבר בדרך.

**מה שכתוב כאן חל על קורסים חדשים.** קורסים קיימים לא מיושרים אחורה. כל הבדיקות
כאן הן **אזהרות ולא שגיאות** (החלטת ינון, 30/07/2026): `node sync.js` ידווח, לא
יחסום. חוב עבר לא עוצר עבודה חדשה, וגם לא נשכח.

לכתיבת השאלות עצמן: **[QUESTION-STANDARD.md](QUESTION-STANDARD.md)**.
לתבנית ה-JSON של שחזור: **[PROMPT.md](PROMPT.md)**.

---

## מה חובה לפני שקורס עולה לאוויר

1. **כרטיס המקצוע** ב-`exams/courses.json`, כולל `about` ורשימת הנושאים.
2. **בנק שאלות אחד לפחות**, עם `topic` ו-`explain` על כל שאלה.
3. **מפת חומרים** (`kind: "guide"`).
4. **סיכום מלא** (`studyDoc`) — קובץ HTML ב-`guides/`.

i❤️Shinun, כרטיסיות, מקרים מתגלגלים, דף נוסחאות וסימולציות הם **אופציונליים**
ולפי אופי הקורס.

---

## שלב 0 — כרטיס המקצוע

בלי זה שום קובץ שאלות לא ייטען: `sync.js` דורש ש-`course` בקובץ המבחן יצביע
למקצוע קיים.

```json
{
  "id": "pharma",
  "name": "פרמקולוגיה",
  "code": "0-471-8-1234",
  "icon": "💊",
  "blurb": "תרופות, קינטיקה ורצפטורים",
  "order": 6, "year": 1, "semester": "ב",
  "status": "active",
  "accent": {
    "light": ["#ראשי", "#כהה", "#רקע-רך"],
    "dark":  ["#ראשי", "#כהה", "#רקע-רך"]
  },
  "dates": [{ "moed": "א", "at": "2027-01-28T09:00" }],
  "about": "מה הקורס, מי המרצה, ואיך נראה המבחן — פסקה או שתיים.",
  "topics": ["<הרשימה הסגורה — ראו שלב 1>"]
}
```

- **`accent` — שלושה צבעים לכל ערכה** (ראשי, כהה, רקע רך). חסר אחד והצבע נשבר
  במצב אחד בלבד, וזה סוג הבאג שלא רואים.
- **`dates` — חובה במקצוע פעיל.** ממנו נבנים הספירה לאחור, "המבחן הבא", מסך
  "הלילה לפני" ודירוג העדיפויות. בלעדיו הקורס קיים אבל שקוף.
- **`about` — חובה.** זה מה שאומר ללומד מה הוא פוגש: פורמט המבחן, מה נבחן ומה
  לא, ומאיפה החומר. קיים היום רק בקליני ובפיזיקה.
- `id` באנגלית, מקפים במקום רווחים. **אין אמנת שמות למזהי הקבצים** (החלטה:
  להשאיר חופשי) — הדרישה היחידה היא ייחודיות. תסתכל על מה שכבר קיים במקצוע
  ותהיה עקבי בתוכו.
- אופציונליים: `flatExams` (פריסת מבחנים בגריד במקום קיבוץ לפי `part`),
  `simExam`, `studyDoc`.

## שלב 1 — רשימת הנושאים, לפני השאלה הראשונה

**`topics` בכרטיס המקצוע היא הרשימה הרשמית והסגורה** — 10–25 נושאים קנוניים,
שנקבעים לפני שנכתבת שאלה אחת. ינון מאשר אותה פעם אחת בהתחלה.

למה זה השלב הכי חשוב: תגית הנושא מפעילה **ארבעה** דברים — הסינון לפי נושא
בתרגול החופשי, הפילוח בסוף המבחן שמראה באילו נושאים נפלת, כפתור "איפה ללמוד"
שמופיע כשטועים, והחיבור הדו-כיווני בין מפת החומרים לשאלות. שם שנכתב אחרת בקובץ
אחר מייצר **נושא שני**, והצ׳יפ שלו מוביל לרשימה ריקה.

`node sync.js` מדווח על שני הכיוונים: נושא שנכתב בקובץ ואינו ברשימה, ונושא
ברשימה שאין לו אף שאלה. **הבדיקה מכסה גם שינון, כרטיסיות ומקרים** — ולא רק
שאלות, כי שם בדיוק נוצר הפיצול: בחפיסת השינון של אלקטרו יש "פוטנציאל פעולה"
ו"ממברנה פסיבית" מול "פוטנציאל הפעולה" ו"תכונות פאסיביות של הממברנה" בשאלות,
ושלוש תגיות שונות לפתולוגיות ורעלנים.

מקצוע בלי `topics` פשוט לא נבדק, וכך הארכיון הקיים לא מוצף. **בקורס חדש זה חובה.**

## שלב 2 — התוכן: השאלות

הסדר שעבד בפיזיקה: קודם מה שקיים, אחר כך מה שנבנה.

1. **שחזורים אמיתיים** (`kind: "shichzur"`) — הנכס האמיתי. ייבוא דרך
   [`import-shichzur`](.claude/skills/import-shichzur/SKILL.md). בשחזור
   **נאמנות למקור גוברת על כל כלל אחר**: לא מיישרים אורכי תשובות, לא מזיזים את
   התשובה הנכונה, ולא מתקנים ניסוח.
2. **בנקי תרגול** (`kind: "practice"`) — כשאין שחזורים, או לכיסוי נושא שאין בו.
   כאן הכללים של [QUESTION-STANDARD.md](QUESTION-STANDARD.md) מחייבים במלואם:
   תשובות באותו אורך, מסיחים שמבלבלים באמת, ופיזור סטוכסטי של התשובה הנכונה.
3. `highyield` נבנה לבד על ידי `repeats.js` כשיש ≥2 מחזורים — לא כותבים ביד.
4. `kind` חוקיים: `shichzur` · `practice` · `highyield` · `cards` · `guide` ·
   `case` · `shinun`.

**שני דברים שהבדיקה תדווח עליהם, ושכדאי מאוד לא להתעלם מהם:**

- **`explain` ו-`topic` על 100% מהשאלות.** בלי נושא אין פילוח ואין תרגול לפי
  נושא; בלי הסבר טעות היא "טעית" ולא "הנה למה". זה סעיף 1 ב-`ROADMAP.md` מאז,
  ובביוכימיה הוא מעולם לא הושלם — 150 שאלות בלי שניהם. אל תיצור חוב חדש מאותו סוג.
- **דו״ח ההטיה נקי.** אם התשובה הנכונה היא הארוכה ביותר ב-78% מהשאלות, הבנק
  פתיר בלי ידע ולא שווה את הזמן שהושקע בו.

## שלב 3 — מפת החומרים (`kind: "guide"`)

יחידה = נושא קנוני מהרשימה. זה מה שמחבר את החומר לשאלות **בשני הכיוונים ובלי
הזנת דאטה**: כפתור "איפה ללמוד" במשוב, ורשימת השאלות בכל יחידה במפה. עמוד
המלכודות נבנה מכאן גם הוא — `trap` בכל נקודה.

לכל יחידה: תמצית פסקה-שתיים, נקודות, מלכודות, וסרטונים.
⚠️ **סרטון נכנס רק אחרי אימות מול התמלול.** ID מומצא הוא הבטחה שבורה.

אם מתכננים `simExam`, המפה צריכה גם **`blocks`** עם הנושאים בכל בלוק — משם
נבנות מכסות השאלות בסימולציה. מפה בלי `blocks` לא תיכשל, היא רק תמלא את
הסימולציה באקראי (זה המצב בביומול).

## שלב 4 — הסיכום המלא (`studyDoc`)

קובץ HTML עצמאי ב-`guides/<course>-full.html`, לפי
[`write-study-guide`](.claude/skills/write-study-guide/SKILL.md), ומחובר דרך
`studyDoc` בכרטיס המקצוע. **שמות הבלוקים והיחידות חייבים להיות זהים למפה** —
אחרת הקישור מהסיכום לתרגול לפי נושא נשבר.

## שלב 5 — שכבות אופציונליות

| שכבה | מה זה | מתי שווה |
|---|---|---|
| i❤️Shinun | `exams/<course>-shinun.json` — עובדות לשינון בעל־פה | קורס עם הרבה שמות, רעלנים, בופרים |
| `cards` | כרטיסיות שהמרצה מסר ישירות | כשיש חומר כזה |
| `case` | מקרים מתגלגלים | מבחן שבנוי על וינייטות, כמו קליני |
| `simExam` | סימולציית מבחן מלאה: מספר שאלות, דקות, ומכסה לכל בלוק | כשמבנה המבחן ידוע |
| דף נוסחאות | דורש עבודה במנוע — ראו שלב 6 | כשמחלקים דף נוסחאות במבחן |
| סימולציות | תופעה שאי אפשר להבין בלי לשחק בה | לפי הקורס |

## שלב 6 — מה שדורש נגיעה בקוד המנוע

רוב הקורס הוא דאטה, וזו הנקודה של הארכיון: קורס חדש מרוויח את כל המנוע בחינם.
**חוץ משלושה דברים, שכולם מוגדרים לפי מזהה הקורס בקוד:**

- **זיהוי שאלות חוזרות ו-High Yield** — `repeats.js` מחזיק טבלה עם ציר הזמן של
  כל מקצוע (שנה מול מחזור) ועם הניסוחים. **מקצוע שלא רשום שם לא מקבל זיהוי
  חזרות ולא מבחן High Yield, בשקט.** זה המצב של ביוכימיה, ו-`sync.js` מזהיר
  עליו מעכשיו. רשומים כרגע: ביומול (ציר מחזור) ואלקטרו (ציר שנה).
- **דף הנוסחאות** — `SHEET` ב-`assets/app.js`: כותרות עמודים, תמונות סרוקות
  ב-`assets/img/<course>-sheet-<n>.png`, ומלבן באחוזים לכל סעיף. **שבב שמצביע
  על הסעיף הלא-נכון גרוע מאין שבב** — כל מלבן נבדק ויזואלית.
  ⚠️ הקישור העצמאי לדף הנוסחאות מוצג היום רק כשלמקצוע יש גם מחולל תרגילים; בלעדיו
  הדף נגיש רק מתוך התרגול ומהמשוב.
- **סימולציות** — `SIMS` ב-`app.js`, שנקשרות לשאלות לפי **התאמת שם נושא מדויקת**.
  שינוי שם נושא מנתק את הקישור בשקט.

## שלב 7 — אימות לפני שזה עולה

```bash
node sync.js
```

חייב לצאת בלי `❌`, וכדאי מאוד לקרוא את האזהרות ולא רק לוודא שאין שגיאות. ואז:

- **לפתוח את הקורס בדפדפן** (`start.command`) ולענות על שאלה בכל בנק חדש:
  שהמשוב נראה נכון, שההסבר נפרס לתבנית, ושהמסיח שנבחר מסומן.
- **ינון עובר על מדגם** לפני שזה עולה לאוויר. הסברים שנכתבו כאן הם ניחוש מלומד
  עד שמישהו שמכיר את החומר אישר אותם — ויש 116 משתמשים בצד השני.
- **לדחוף רק אחרי `node sync.js`**, כי הוא זה שמחתים את `index.html` מחדש. בלי
  החתימה, מי שכבר היה באתר מקבל את הגרסה הישנה מהמטמון.
